version: '3.8'

services:
  refook:
    image: refook-front
    build: .
    container_name: refook-front
    ports:
      - "8080:80"
    volumes:
      - ./dist:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./start.sh:/start.sh